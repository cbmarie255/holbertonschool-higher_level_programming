#!/usr/bin/python3
"""
    Returns True if the object is an instance of, or the object is an instance
"""


def is_kind_of_class(obj, a_class):
    """Checks for instance"""
    return isinstance(obj, a_class)
